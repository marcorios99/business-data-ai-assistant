# Semantic Layer

Planned semantic domains:

- sales
- inventory
- procurement
- returns
- performance

Each domain will expose a small curated set of metrics, dimensions, filters, aliases, and allowed relationships. Physical schema metadata can be discovered automatically, while business semantics remain explicit and reviewable.
